* Find a better way to handle the <=> operator for the solver nodes
* See if algorithm gem's tree is any faster than rbtree gem's for SortedQueue
* Look for ways to reduce object duplication
* Create method to serialize/deserialize boards so they can be written and read from files (json/yaml?)
